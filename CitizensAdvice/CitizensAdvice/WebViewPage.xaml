<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="CitizensAdvice.WebViewPage">
    <ContentPage.Content>

        <StackLayout>
            <Grid Margin="10" VerticalOptions="StartAndExpand">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition />
                </Grid.ColumnDefinitions>
                <Button Grid.Column="0" Text="Back" HeightRequest ="80" Command="{Binding GoBackCommand}"/>
                <Button Grid.Column="1" Text="Forward" HeightRequest="80" Command="{Binding GoForwardCommand}" />
            </Grid>
            <WebView x:Name="WebView1"  Source="{Binding Url}" HeightRequest="1000" WidthRequest="1000" Navigated="WebView1_OnNavigated"/>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>